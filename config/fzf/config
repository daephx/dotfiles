# Fzf configuration: A command-line fuzzy finder
# https://github.com/junegunn/fzf

# Display height relative to the terminal size
--height=~100%

# Highlight the whole line for current selection
--highlight-line

# Display info text on the right side of the prompt
--info=inline-right

# Layout configuration: reverse order (from bottom to top)
--layout=reverse

# Margin settings for window edges: top,right,bottom,left
--margin=0

# Padding settings: top,bottom
--padding=0,1

# Set the pointer character
--pointer='❯'

# Preview window settings: border on the left side
--preview-window="sharp,<80(down,50%)"

# Set the prompt character
--prompt='❯ '

# Separator between the preview and search results
--separator=''

# Set color scheme to dark
--color=dark

# Color configurations for various UI elements
--color=bg+:8
--color=bg:-1
--color=border:8
--color=fg+:15
--color=fg:7
--color=gutter:-1
--color=header:3
--color=hl+:3
--color=hl:6
--color=info:3
--color=marker:6
--color=pointer:3
--color=preview-fg:15
--color=prompt:4
--color=spinner:3

# Move to the first item on change
--bind='change:first'

# Additional key bindings for feature toggles
--bind='alt-a:toggle-all'
--bind='alt-g:ignore'
--bind='alt-p:toggle-preview'
--bind='alt-s:toggle-sort'
--bind='alt-y:yank'

# Key bindings for preview navigation and refreshing
--bind='ctrl-b:preview-half-page-up'
--bind='ctrl-d:preview-half-page-down'
--bind='ctrl-j:preview-down'
--bind='ctrl-k:preview-up'
--bind='ctrl-l:clear-screen'
--bind='ctrl-u:unix-line-discard'

# Key bindings for navigating through items
--bind='pgdn:half-page-down+refresh-preview'
--bind='pgup:half-page-up+refresh-preview'
