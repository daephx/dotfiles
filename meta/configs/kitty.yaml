# Install kitty terminal

- create:
    - ~/.config/kitty

- link:
    ~/.config/kitty:
      glob: true
      path: config/kitty/*

- sudo:
  - shell:
    - [apt install -y kitty, Installing package; Kitty Terminal]
